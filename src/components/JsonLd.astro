---
/**
 * Injects a JSON-LD structured-data block (Organization + Person + WebSite graph).
 * Site-wide schema with stable @id references.
 */
import { BRAND, SITE, PERSON, CONTACT } from '../site.config';

const siteUrl = SITE.url.replace(/\/$/, '');
const orgId = `${siteUrl}/#org-1pointo`;
const websiteId = `${siteUrl}/#website`;
const personId = `${siteUrl}/#person-ahron-darnell`;

const graph = {
  '@context': 'https://schema.org',
  '@graph': [
    {
      '@type': 'Organization',
      '@id': orgId,
      name: BRAND.name,
      alternateName: SITE.name,
      url: siteUrl,
      description: SITE.description,
      foundingLocation: { '@type': 'Place', address: CONTACT.location },
      address: {
        '@type': 'PostalAddress',
        addressLocality: 'Buffalo',
        addressRegion: 'NY',
        addressCountry: 'US',
      },
      employee: { '@id': personId },
      knowsAbout: PERSON.knowsAbout,
      sameAs: PERSON.sameAs,
    },
    {
      '@type': 'Person',
      '@id': personId,
      name: PERSON.name,
      givenName: PERSON.givenName,
      familyName: PERSON.familyName,
      jobTitle: PERSON.jobTitle,
      url: siteUrl,
      image: `${siteUrl}${PERSON.image}`,
      sameAs: PERSON.sameAs,
      knowsAbout: PERSON.knowsAbout,
      workLocation: {
        '@type': 'Place',
        address: {
          '@type': 'PostalAddress',
          addressLocality: 'Williamsville',
          addressRegion: 'NY',
          addressCountry: 'US',
        },
      },
      worksFor: { '@id': orgId },
    },
    {
      '@type': 'WebSite',
      '@id': websiteId,
      url: siteUrl,
      name: `${BRAND.name} — ${SITE.name}`,
      alternateName: SITE.title,
      description: SITE.description,
      publisher: { '@id': orgId },
      inLanguage: 'en-US',
    },
  ],
};
---

<script is:inline type="application/ld+json" set:html={JSON.stringify(graph)} />
